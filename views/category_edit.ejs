<!DOCTYPE html>
<html lang="he">
<head>
    <meta charset="UTF-8">
    <title>עריכת קטגוריה</title>
    <style>
        body {
            direction: rtl;
            font-family: Arial, sans-serif;
            padding: 20px;
        }

        form {
            max-width: 400px;
            margin: auto;
            display: flex;
            flex-direction: column;
            gap: 10px;
        }

        label {
            font-weight: bold;
        }

        input[type="text"] {
            padding: 8px;
            font-size: 1em;
        }

        .actions {
            text-align: center;
        }
    </style>
</head>
<body>

<h2 style="text-align:center;">✏️ עריכת קטגוריה</h2>

<form action="/C/Edit" method="POST">
    <input type="hidden" name="id" value="<%= category.id %>">

    <label>שם קטגוריה:</label>
    <input type="text" name="name" value="<%= category.name %>" required>

    <div class="actions">
        <button type="submit">💾 שמור</button>
        <a href="/C">🔙 חזור לרשימה</a>
    </div>
</form>

</body>
</html>
