<!doctype html>
<html lang="he">
<head>
    <meta charset="UTF-8">
    <title>רשימת קטגוריות</title>
    <style>
        body {
            direction: rtl;
            font-family: Arial, sans-serif;
        }
        table {
            width: 60%;
            margin: 20px auto;
            border-collapse: collapse;
        }
        th, td {
            border: 1px solid #aaa;
            padding: 10px;
            text-align: center;
        }
        th {
            background-color: #eee;
        }
        form {
            margin: 0;
        }
        a.button {
            display: inline-block;
            padding: 5px 10px;
            background-color: #4CAF50;
            color: white;
            text-decoration: none;
            border-radius: 4px;
        }
        button {
            background-color: #f44336;
            color: white;
            border: none;
            padding: 5px 10px;
            border-radius: 4px;
            cursor: pointer;
        }
    </style>
</head>
<body>
<h2 style="text-align:center;">רשימת קטגוריות</h2>

<div style="text-align:center; margin-bottom: 20px;">
    <a class="button" href="/C/Add">➕ הוספת קטגוריה חדשה</a>
</div>

<table>
    <thead>
    <tr>
        <th>שם</th>
        <th>עריכה</th>
        <th>מחיקה</th>
    </tr>
    </thead>
    <tbody>
    <% for (let cat of categories) { %>
        <tr>
            <td><%= cat.name %></td>

            <td>
                <a class="button" href="/C/Edit/<%= cat.id %>"> ערוך</a>
            </td>

            <td>
                <form action="/C/Delete" method="POST" onsubmit="return confirm('בטוח/ה שברצונך למחוק?')">
                    <input type="hidden" name="id" value="<%= cat.id %>">
                    <button type="submit"> מחק</button>
                </form>
            </td>
        </tr>
    <% } %>
    </tbody>
</table>

</body>
</html>
